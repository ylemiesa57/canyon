{
  "name": "canyon",
  "version": "0.1.0",
  "private": true,
  "description": "Canyon: landing page and buyer/shop app mockups. Static site, no build step.",
  "scripts": {
    "check": "node tests/check.js",
    "test": "node tests/smoke.js",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
